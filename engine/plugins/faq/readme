# =========================================================================== #
# BixBite CMS // Плагины // Вопросы и ответы                                  #
# =========================================================================== #

Страницы плагина:
  . страница со списком - /plugin/faq/ (TWIG шаблон faq_page.tpl)
  . страница добавления списком - /plugin/faq/add/ (TWIG шаблон faq_add.tpl)

--------------------------------------------------------------------
Поддерживаемые переменные:
  . {{tpl_url}} - путь к текущему активному шаблону сайта

* Цикл {% for entry in entries %}{% endfor %} - шаблон содержимого (строки) информационного блока
  Поддерживаемые переменные:
  . {{entry.id}}   - id вопрос-ответа
  . {{entry.question}}   - текст вопроса
  . {{entry.answer}}     - текст ответа
  
Вывод вопрос/ответ в блоке (по умолчанию TWIG шаблон faq_block.tpl).
--------------------------------------------------------------------
Поддерживаемые переменные:
  . {{tpl_url}} - путь к текущему активному шаблону сайта

* Цикл {% for entry in entries %}{% endfor %} - шаблон содержимого (строки) информационного блока
  Поддерживаемые переменные:
  . {{entry.id}}   - id вопрос-ответа
  . {{entry.question}}   - текст вопроса
  . {{entry.answer}}     - текст ответа
  
Пример блока (для TWIG шаблона):
{{ callPlugin('faq.show', {'maxnum' : 3, 'template': 'faq_block', 'order' : 'ASC', 'cache_expire': '360'}) }}
  Поддерживаемые параметры:
  . maxnum   	- число записей в блоке
  . template 	- имя шаблона
  . order    	- сортировка записей: ASC / DESC
  . cache_expire - время кеша