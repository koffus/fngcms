/* Hungarian initialisation for the jQuery UI multiselect plugin. */
/* Written by Adam FГіnagy (adam.fonagy@greenformatics.hu). */

(function ( $ ) {

$.extend($.ech.multiselectfilter.prototype.options, {
	label: "KeresГ©s:",
	placeholder: "kulcsszГі megadГЎsa"
});

})( jQuery );
